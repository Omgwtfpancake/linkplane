# Prototype snapshot

These files preserve the original September 2026 PhoneBridge prototype unchanged. They
are retained as implementation history; the maintained CLI lives under `src/phonebridge`.

- `linux/` contains the workstation commands and user systemd service.
- `termux/` contains the scripts recovered from the connected Android device.

SSH keys and generated battery data are intentionally not stored in Git.
