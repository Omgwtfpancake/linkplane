"""Linkplane core models: the public, provider-independent vocabulary.

Only the v0.1 subset lives here (capability catalogue, structured errors). Device, result,
progress, and configuration models already exist in the top-level modules (`models.py`,
`operations.py`, `transports.py`/`profiles.py`) and are re-used, not duplicated.
"""
