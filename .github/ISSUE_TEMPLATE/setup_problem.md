---
name: Setup or install problem
about: Installation or `linkplane setup` did not get to "Your phone is ready"
labels: setup
---

**OS / version**:
**Install command used**:
**`linkplane --version`** (or "command not found"):

**Where setup stopped** (the line, the `Error: LP-…` code, and the "Next step"):

**Phone state** (plugged in? USB debugging on? did the phone show "Allow USB debugging?"):

**`linkplane setup --json`** and **`linkplane doctor --json`** (attach; replace the serial with DEVICE_SERIAL):
